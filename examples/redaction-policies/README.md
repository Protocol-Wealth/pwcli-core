# Redaction Policy Examples

Synthetic policy examples for declaring how sensitive data should be handled
before prompts, tool calls, artifacts, external calls, logs, and memory writes.

These examples are control-plane metadata. Host applications must still enforce
the policies in code.
