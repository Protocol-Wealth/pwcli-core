# Runtime Adapter Examples

Synthetic metadata examples for mapping existing agent runtimes and protocols
into the `pwcli-core` control plane. These files are references, not runtime
configuration that grants permissions.

Each fixture declares a `schemaRef` to `schemas/runtime-adapter.schema.json` and
wraps one adapter example.
